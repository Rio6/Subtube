{
    "name": "express-server",
    "version": "0.1.0",
    "description": "Static express server",
    "main": "server.js",
    "scripts": {
        "build": "mkdir -p dist && babel src -d dist && cp -u index.html dist"
    },
    "author": "Rio",
    "dependencies": {
        "@babel/cli": "^7.8.4",
        "@babel/core": "^7.9.6",
        "@babel/preset-env": "^7.9.6",
        "express": "^4.17.1"
    }
}
